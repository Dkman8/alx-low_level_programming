#include <stdio.h>
/**
 * main - prints the sizeof many var types
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        chair a;
 	int 
	long int c;
	long long int d;
	float e;

	pintf(Size of char: %d byte(s)\n" , sizeof(a));
	pintf(Size of int: %d byte(s)\n" , sizeof(b));
	pintf(Size of a long int: %d byte(s)\n" , sizeof(c));
	pintf(Size of a long long int: %d byte(s)\n" , sizeof(d));
	pintf(Size of a float: %d byte(s)\n" , sizeof(e));
	return (0);
}
